---
phase: 03-query-data-workflow
plan: 05
type: execute
wave: 1
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - public/js/studio.js
  - public/js/query-explorer.js
  - public/studio.html
autonomous: false
gap_closure: true
requirements:
  - DSRC-01
  - DSRC-02
  - QRYX-01
  - QRYX-02

must_haves:
  truths:
    - "User expands the HEALTH collapsible in the Sources tab and sees rows for all 18 data sources with status dots, names, timestamps, and error badges"
    - "User opens the Query Explorer modal, enters a GCP metric type, clicks Run, and sees raw results rendered in the results pane"
    - "User clicks a data source row in the Sources tab, sees the datasource editor panel open with query list, clicks Edit Credentials, and sees the credential form with input fields"
  artifacts:
    - path: "public/js/studio.js"
      provides: "Fixed _renderHealthSection, openDatasourceEditor, _loadCredentialForm runtime flows"
      contains: "_renderHealthSection"
    - path: "public/js/query-explorer.js"
      provides: "Fixed QueryExplorer._runQuery runtime flow"
      contains: "_runQuery"
  key_links:
    - from: "public/js/studio.js"
      to: "/api/data-sources/health"
      via: "fetch in _renderHealthSection populates health-section-content"
      pattern: "fetch.*api/data-sources/health"
    - from: "public/js/query-explorer.js"
      to: "/api/explore/gcp"
      via: "fetch in _runQuery posts query and renders results"
      pattern: "fetch.*api/explore"
    - from: "public/js/studio.js"
      to: "/api/data-sources/schemas"
      via: "fetch in _loadCredentialForm renders credential input fields"
      pattern: "fetch.*api/data-sources/schemas"
---

<objective>
Fix three confirmed runtime bugs in the Phase 3 admin UI: (1) health section renders empty, (2) query explorer execution/results fail, (3) credential editor panel does not show the form.

Purpose: All Phase 3 code was written and committed, and all APIs return correct data, but three features fail at runtime in the browser. This plan debugs and fixes each failure.
Output: All three features work correctly in the browser at http://tv:3000/admin.
</objective>

<execution_context>
@/home/tech/.claude/get-shit-done/workflows/execute-plan.md
@/home/tech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-query-data-workflow/03-CONTEXT.md
@.planning/phases/03-query-data-workflow/03-VERIFICATION.md
@.planning/phases/03-query-data-workflow/03-02-SUMMARY.md
@.planning/phases/03-query-data-workflow/03-03-SUMMARY.md
@.planning/phases/03-query-data-workflow/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debug and fix health section empty rendering (DSRC-01)</name>
  <files>public/js/studio.js, public/studio.html</files>
  <read_first>
    - public/js/studio.js (lines 3489-3576 -- _renderHealthSection, _startHealthPolling, _stopHealthPolling — the health rendering code)
    - public/js/studio.js (lines 2220-2232 -- bindSidebarTabs — where _startHealthPolling is called)
    - public/js/studio.js (lines 1836-1853 -- bindCollapsibles — how collapsible toggling works)
    - public/studio.html (lines 107-114 -- panel-datasources containing health-section-header, health-section-content, datasource-list)
    - public/css/studio.css (search for "health-row" and "health-section" -- check for CSS rules that might hide content)
  </read_first>
  <action>
This is a runtime debugging task. The code for _renderHealthSection exists and the /api/data-sources/health API returns valid data for 18 sources. The health-section-content div starts display:none (correct — collapsed). _startHealthPolling calls _renderHealthSection which populates the div. bindCollapsibles toggles it to display:block on header click.

**Debugging approach — add temporary console.log tracing to identify the failure point:**

1. Open browser devtools at http://tv:3000/admin and check the console for errors when clicking the Sources tab and expanding the HEALTH section.

2. If no visible error, add console.log tracing to `_renderHealthSection()` in public/js/studio.js:
   - At the start: `console.log('[health] _renderHealthSection called');`
   - After `const content = document.getElementById('health-section-content');`: `console.log('[health] content element:', content);`
   - After `const data = await res.json();`: `console.log('[health] API response:', data);`
   - Inside the forEach: `console.log('[health] rendering row:', sourceName);`
   - In the catch: `console.log('[health] fetch error:', e);`

3. Restart the server: `sudo systemctl restart tv-dashboards`

4. Open http://tv:3000/admin in a browser, click Sources tab, check console output.

5. Based on console output, identify and fix the specific failure. Common causes:
   - **Hypothesis A**: The content div `health-section-content` may not be found because the panel-datasources is display:none when bindCollapsibles runs (but getElementById should still find it).
   - **Hypothesis B**: The fetch response may be wrapped in an Elysia validation error object `{ type: 'validation', ... }` instead of `{ success: true, health: {...} }`. Check if the response has a `type` field — if so, the TypeBox model on the health endpoint is rejecting the response shape.
   - **Hypothesis C**: A JS error in the forEach loop (e.g., accessing a property of undefined) may silently prevent rendering. Check if any source entry in the health response has an unexpected shape.
   - **Hypothesis D**: CSS is hiding the rows — check if `.health-row` has `display:none` or visibility rules that prevent rendering.

6. After identifying the root cause, apply the fix. Then remove all console.log tracing lines.

7. Restart: `sudo systemctl restart tv-dashboards`

**IMPORTANT:** If the root cause is Elysia schema validation wrapping the response, the fix is in `server/models/data-source.model.js` or `server/index.js` — ensure the response schema matches the actual data shape exactly (use t.Nullable for null fields, t.Optional for optional fields).

**IMPORTANT:** If the issue is that _renderHealthSection populates content while it's display:none and something clears it before the user expands — check if renderDatasourceList() or any other function clears the health-section-content element.
  </action>
  <verify>
    <automated>curl -s http://tv:3000/api/data-sources/health | python3 -c "import json,sys; d=json.load(sys.stdin); assert d.get('success') == True; assert len(d.get('health',{})) > 0; print('API OK:', len(d['health']), 'sources')"</automated>
  </verify>
  <acceptance_criteria>
    - public/js/studio.js _renderHealthSection method exists and has no console.log debug lines remaining
    - curl http://tv:3000/api/data-sources/health returns JSON with success:true and health object containing 18 sources (no Elysia validation wrapping)
    - The health-section-content element is populated with .health-row children after _renderHealthSection completes (verified by browser inspection at checkpoint)
  </acceptance_criteria>
  <done>Health section in Sources tab displays rows for all data sources when the HEALTH collapsible is expanded — each row shows status dot, source name, timestamp, and error badge (if applicable)</done>
</task>

<task type="auto">
  <name>Task 2: Debug and fix Query Explorer runtime failure (QRYX-01, QRYX-02)</name>
  <files>public/js/query-explorer.js, public/studio.html</files>
  <read_first>
    - public/js/query-explorer.js (full file — the entire QueryExplorer class: constructor, _init, open, _onSourceChange, _runQuery, _buildGcpBody, _renderGcpResults, _renderBqResults, _buildTable, _buildResultsHeader, _showError, _renderWidgetPreview, _setActionsDisabled)
    - public/studio.html (lines 822-990 -- qx-modal full HTML — verify all element IDs match what JS expects)
    - server/explore-routes.js (full file — verify API response shapes match what _renderGcpResults and _renderBqResults expect)
    - public/css/query-explorer.css (check for display rules that might hide results)
  </read_first>
  <action>
This is a runtime debugging task. The QueryExplorer modal opens (confirmed). The /api/explore/gcp endpoint returns valid data (confirmed by curl test). The JS code for _runQuery, result rendering, and all supporting methods exists. The failure is somewhere in the browser execution.

**Debugging approach:**

1. Open browser devtools at http://tv:3000/admin. Click the diamond Explorer button in the Queries tab to open the Query Explorer modal.

2. In the console, check if the QueryExplorer instance exists:
   ```
   document.querySelector('#qx-modal')  // should exist
   document.querySelector('#qx-run-btn')  // should exist
   ```

3. Enter a known-good metric type (e.g. `run.googleapis.com/request_count`) and click Run. Watch the console for errors.

4. If no visible error, add console.log tracing to `_runQuery()` in public/js/query-explorer.js:
   - At the start: `console.log('[qx] _runQuery called, source:', this._source?.value);`
   - After body construction: `console.log('[qx] body:', body);`
   - After fetch response: `console.log('[qx] response:', data);`
   - Before rendering: `console.log('[qx] rendering results, success:', data.success);`
   - In catch: `console.log('[qx] error:', err);`

5. Restart server and test again. Based on console output, identify the failure point.

**Common failure hypotheses:**
   - **Hypothesis A**: The `_source` select element is not found or returns undefined, causing the early return on line 117 (`if (!src || ...)`). Check that `#qx-source` select has the correct initial value.
   - **Hypothesis B**: The fetch to `/api/explore/gcp` may be blocked by CORS or fail silently. Check the Network tab.
   - **Hypothesis C**: The `_renderGcpResults` method may throw when processing `rawSeries` — e.g., if rawSeries is an empty array or has unexpected object structure.
   - **Hypothesis D**: The `_buildTable` method may throw if `rows[0]` is undefined (empty results).
   - **Hypothesis E**: The `_renderWidgetPreview` method may throw if `window.Widgets` is undefined or if the widget type is not supported, crashing the entire try block.
   - **Hypothesis F**: A script load order issue — query-explorer.js may load before studio.js, causing `window.QueryExplorer` to be undefined when StudioApp tries to instantiate it. Check the script tag order in studio.html.

6. After identifying the root cause, apply the fix. Remove all console.log tracing.

7. Restart: `sudo systemctl restart tv-dashboards`

**IMPORTANT:** The widget-scoped _runQuery in studio.js (line 2366) is a DIFFERENT method from QueryExplorer._runQuery. Both may need fixes. The widget-scoped one calls `/api/queries/:source/:id/preview` which requires a saved query. The QueryExplorer one calls `/api/explore/:source` which is ad-hoc. Both should work independently.
  </action>
  <verify>
    <automated>curl -s -X POST http://tv:3000/api/explore/gcp -H 'content-type: application/json' -d '{"metricType":"run.googleapis.com/request_count","project":"mad-master","timeWindow":5}' | python3 -c "import json,sys; d=json.load(sys.stdin); assert d.get('success') == True; assert d.get('seriesCount',0) > 0; print('API OK:', d['seriesCount'], 'series,', d['pointCount'], 'points')"</automated>
  </verify>
  <acceptance_criteria>
    - public/js/query-explorer.js _runQuery method exists and has no console.log debug lines remaining
    - POST /api/explore/gcp returns success:true with rawSeries array (verified by curl)
    - QueryExplorer modal opens, user can enter a metric type, click Run, and see results rendered (verified by browser inspection at checkpoint)
  </acceptance_criteria>
  <done>Query Explorer modal allows entering a GCP metric type, running the query, and seeing raw results (time series table) and widget preview rendered in the results pane</done>
</task>

<task type="auto">
  <name>Task 3: Debug and fix credential editor panel (DSRC-02)</name>
  <files>public/js/studio.js, public/studio.html</files>
  <read_first>
    - public/js/studio.js (lines 3176-3211 -- renderDatasourceList — how source rows are created with click handlers)
    - public/js/studio.js (lines 3213-3252 -- openDatasourceEditor — how the editor panel is shown and wired)
    - public/js/studio.js (lines 3254-3266 -- _showDseQueryView, _showDseCredView — panel toggle)
    - public/js/studio.js (lines 3329-3448 -- _loadCredentialForm — how credential input fields are rendered)
    - public/js/studio.js (lines 3454-3470 -- _validateCredForm — validation logic)
    - public/studio.html (lines 598-628 -- datasource-editor-panel HTML with dse-query-view, dse-cred-view, dse-fields, dse-edit-creds button)
    - server/data-source-schemas.js (verify /api/data-sources/schemas returns correct shape)
  </read_first>
  <action>
This is a runtime debugging task. The datasource editor HTML, JS methods, and API endpoints all exist. The /api/data-sources/schemas endpoint returns valid field schemas. The user reported "no way to view or save credentials."

**Debugging approach:**

1. Open browser devtools at http://tv:3000/admin. Click Sources tab. Click any data source row (e.g., vulntrack).

2. Check: Does the datasource-editor-panel appear? If not, `openDatasourceEditor` may be failing. Check console for errors.

3. If the panel appears with the query list, click the "Edit Credentials" button. Check: Does the credential form (dse-cred-view) appear? If not, `_showDseCredView` or `_loadCredentialForm` is failing.

4. Add console.log tracing to the flow:
   - In `openDatasourceEditor`: `console.log('[dse] openDatasourceEditor called for:', src.name);`
   - After showing the panel: `console.log('[dse] panel displayed');`
   - In the editCredsBtn onclick: `console.log('[dse] Edit Credentials clicked');`
   - In `_showDseCredView`: `console.log('[dse] _showDseCredView called');`
   - In `_loadCredentialForm`: `console.log('[dse] _loadCredentialForm called for:', src.name);`
   - After schema fetch: `console.log('[dse] schema response:', schemaData);`
   - After field rendering: `console.log('[dse] rendered', (schema.fields || []).length, 'fields');`

5. Restart server and test. Based on console output, identify the failure.

**Common failure hypotheses:**
   - **Hypothesis A**: The `datasource-editor-panel` is inside the right `<aside class="studio-properties">` panel. The `openDatasourceEditor` method hides `properties-placeholder`, `properties-content`, and `query-editor-panel`, then shows `datasource-editor-panel`. But if the aside itself is not visible (e.g., if no widget is selected and the aside is collapsed), the panel won't show. Check if the aside has display:none when no widget is selected.
   - **Hypothesis B**: The `_loadSourceQueries` method calls `GET /api/queries/:sourceName` which may return an error if the source name doesn't match a known source in queries.yaml. If the fetch fails, the `_loadSourceQueries` catch block shows "Failed to load queries" but the panel should still appear.
   - **Hypothesis C**: The `_loadCredentialForm` fetch to `/api/data-sources/schemas` may be failing or returning an empty schema for some sources. Verify with `curl http://tv:3000/api/data-sources/schemas`.
   - **Hypothesis D**: The `dse-edit-creds` button onclick handler is set inside `openDatasourceEditor` but may be overwritten or not firing due to event propagation issues.
   - **Hypothesis E**: The `dse-query-view` and `dse-cred-view` display toggles use `style.display = 'flex'` and `'none'` but the elements don't have CSS that works with flex display — they may need `display: block` instead.

6. After identifying the root cause, apply the fix. Remove all console.log tracing.

7. Restart: `sudo systemctl restart tv-dashboards`

**IMPORTANT:** Test the schemas endpoint first:
```
curl -s http://tv:3000/api/data-sources/schemas | python3 -m json.tool | head -30
```
If this returns an error or empty schemas, the issue is server-side, not client-side.
  </action>
  <verify>
    <automated>curl -s http://tv:3000/api/data-sources/schemas | python3 -c "import json,sys; d=json.load(sys.stdin); schemas=d.get('schemas',{}); assert len(schemas) > 0; print('Schemas OK:', len(schemas), 'sources'); [print(' ', k, len(v.get('fields',[])), 'fields') for k,v in list(schemas.items())[:5]]"</automated>
  </verify>
  <acceptance_criteria>
    - public/js/studio.js openDatasourceEditor, _showDseCredView, _loadCredentialForm methods exist and have no console.log debug lines remaining
    - curl http://tv:3000/api/data-sources/schemas returns JSON with schemas object containing field definitions for multiple sources
    - Clicking a source row in the Sources tab opens the datasource editor panel (verified by browser inspection at checkpoint)
    - Clicking "Edit Credentials" shows the credential form with input fields (verified by browser inspection at checkpoint)
  </acceptance_criteria>
  <done>Credential editor opens when user clicks a data source and then "Edit Credentials"; form shows input fields from schema; validation blocks empty required fields with inline errors; save sends PUT to /api/data-sources/:name/credentials</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify all three gap fixes work in the browser</name>
  <files>public/js/studio.js, public/js/query-explorer.js</files>
  <action>Human verifies the three bug fixes by testing each flow end-to-end in the admin UI at http://tv:3000/admin</action>
  <what-built>
    Debugged and fixed three runtime bugs:
    1. Health section now displays source rows when HEALTH collapsible is expanded
    2. Query Explorer now executes queries and renders results
    3. Credential editor now opens and shows input fields
  </what-built>
  <how-to-verify>
    1. Open http://tv:3000/admin

    2. **Health section (DSRC-01):** Click Sources tab. Click the HEALTH header to expand it. Verify you see rows for all data sources (gcp, mock, vulntrack, bigquery, etc.) with status dots, names, and timestamps. Wait 30 seconds — the poll dot should pulse and rows should refresh.

    3. **Query Explorer (QRYX-01, QRYX-02):** Click Queries tab. Click the diamond Explorer button. In the modal, ensure Source is "GCP Metrics". Enter `run.googleapis.com/request_count` as the metric type. Click Run. Verify results appear in the results pane (a table of time series data). Verify the widget preview renders on the right.

    4. **Credential Editor (DSRC-02):** Click Sources tab. Click any connected source row (e.g., vulntrack). Verify the datasource editor panel opens showing queries. Click "Edit Credentials". Verify input fields appear. Clear a required field and click Save — verify inline "Required." error appears without a network request.
  </how-to-verify>
  <verify>Human confirms all three flows work correctly</verify>
  <done>All three gap fixes verified: health section shows rows, query explorer executes and renders results, credential editor opens with input fields</done>
  <resume-signal>Type "approved" or describe remaining issues</resume-signal>
</task>

</tasks>

<verification>
- `curl -s http://tv:3000/api/data-sources/health | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['success']; print('Health OK:', len(d['health']), 'sources')"` — health API returns valid data
- `curl -s -X POST http://tv:3000/api/explore/gcp -H 'content-type: application/json' -d '{"metricType":"run.googleapis.com/request_count","project":"mad-master","timeWindow":5}' | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['success']; print('Explore OK:', d['seriesCount'], 'series')"` — explore API returns valid data
- `curl -s http://tv:3000/api/data-sources/schemas | python3 -c "import json,sys; d=json.load(sys.stdin); print('Schemas OK:', len(d.get('schemas',{})), 'sources')"` — schemas API returns valid data
- Human verifies all three features at checkpoint
</verification>

<success_criteria>
- Health section displays source rows when expanded in the Sources tab
- Query Explorer modal can execute queries and display results
- Credential editor opens and shows input fields when "Edit Credentials" is clicked
- No console.log debug lines remain in the code
- All existing unit tests still pass: `bun test tests/unit --timeout 10000`
</success_criteria>

<output>
After completion, create `.planning/phases/03-query-data-workflow/03-05-SUMMARY.md`
</output>
